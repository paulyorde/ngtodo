import { CurrentDirective } from './current.directive';

describe('CurrentDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentDirective();
    expect(directive).toBeTruthy();
  });
});
